# Notebooks
LatentSync_1_6.ipynb
voxcpm.ipynb
video_subtitle_remover.ipynb
.ipynb_checkpoints/

# IDE and environment variables
.cursor/
.env
.env.*
# ...but the template carries no secrets and must be in the repo.
!.env.example

# Model checkpoint files
*.pt
# ...except the subtitle remover's Big-LAMA, which is split into five parts
# and committed like the ProPainter .pth files next to it. Without these a
# fresh clone has no inpainting model and every subtitle removal fails.
!video-subtitle-remover/backend/models/big-lama/big-lama_?.pt

# folder not need to be tracked
myenv/
label-studio/
spy-ads-creative-desktop-tool/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Dub server runtime
jobs/

myenv311/

# What every take measured, used to fit the duration model.
data/
