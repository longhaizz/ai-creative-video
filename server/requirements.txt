# ===========================================================================
# Dub server — main venv (/opt/venv-main).
#
# VSR runs in its OWN venv (server/requirements-vsr.txt, step 7). The reason
# is numpy: 1.26 here, but paddle in VSR needs 2.2, and the two ABIs clash.
#
# torch / VoxCPM / LatentSync / faster-whisper / demucs arrive in step 8,
# when real code calls them. We install them late on purpose, so that steps
# 3-5 still run their tests on a machine with no GPU.
# ===========================================================================

fastapi
uvicorn[standard]
python-multipart

# --------------------------------------------------------------------------
# [DEV] For tests only. Not needed on the production server.
#   httpx: fastapi.testclient does not work without it.
# --------------------------------------------------------------------------
pytest
httpx
