# ===========================================================================
# Dub server - main venv (/opt/venv-main), the HTTP side.
#
# The models are NOT here. They live in server/requirements-models.txt, and
# that split is deliberate: the queue, the API, the cancel path, the timing
# maths and the subtitle work all have tests that run on a laptop with no
# GPU. Putting torch in this file would end that.
#
#   pip install -r server/requirements.txt          # to work on the API
#   pip install -r server/requirements-models.txt   # to run real jobs
#
# The subtitle remover has a third venv of its own, because its numpy does
# not fit next to LatentSync. See server/requirements-vsr.txt.
# ===========================================================================

fastapi
uvicorn[standard]
python-multipart

# Used by the OpenAI calls in server/steps/translate.py.
requests

# --------------------------------------------------------------------------
# [DEV] For tests only. Not needed on the production server.
#   httpx: fastapi.testclient does not work without it.
# --------------------------------------------------------------------------
pytest
httpx
