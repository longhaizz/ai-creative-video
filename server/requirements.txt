# ===========================================================================
# Dub server — venv chính (/opt/venv-main).
#
# VSR chạy venv RIÊNG (server/requirements-vsr.txt, bước 7) vì numpy 1.26 ở
# đây xung đột ABI với numpy 2.2 mà paddle của VSR đòi.
#
# torch / VoxCPM / LatentSync / faster-whisper / demucs vào ở bước 8, khi có
# code thật gọi tới. Chưa cài sớm để bước 3-5 test được trên máy không GPU.
# ===========================================================================

fastapi
uvicorn[standard]
python-multipart

# --------------------------------------------------------------------------
# [DEV] Chỉ để chạy test. Không cần trên server production.
#   httpx: fastapi.testclient bắt buộc phải có.
# --------------------------------------------------------------------------
pytest
httpx
